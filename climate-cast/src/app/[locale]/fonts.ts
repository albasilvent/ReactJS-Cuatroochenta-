import { Lato } from "next/font/google";

export const lato = Lato({
    subsets: ["latin"],
    weight: ["900"],
});

export const lato_light = Lato({
    subsets: ["latin"],
    weight: ["400"],
});
